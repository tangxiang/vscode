const val = 1;
let name1 ='alex';
let age = 31;
let colors = ['black', 'red', 'blue'];
let person = {
    name: 'iris',
    age: 32,
    colors: ['green', 'white']
}

person.name = name1;
person.colors = colors;

console.log(colors);
console.log(person);

console.log(person.age);
console.log(person.name);